# Argosbx Configuration Template
# 使用说明:
# 1. 复制此文件为 config.env: cp config.env.example config.env
# 2. 修改所需的变量值
# 3. 将 config.env 放入 $HOME/agsbx/ 目录
# 4. 运行 Docker 命令挂载该目录即可

# ==========================================
# 基础配置
# ==========================================
# 用户 UUID (留空则自动生成随机 UUID)
uuid=""

# 手动指定公网 IP (可选，用于解决 Docker 内无法获取宿主机 IP 的问题)
# 如果留空，脚本会尝试自动获取
ippz=""

# ==========================================
# 协议开关 (yes/no)
# 推荐组合: vlp=yes, hyp=yes, tup=yes
# ==========================================

# [推荐] VLESS-Reality (适合大多数环境)
vlp="yes"
# [推荐] Hysteria2 (暴力加速，适合线路差)
hyp="yes"
# [推荐] Tuic V5 (高性能 Quic)
tup="yes"

# VMess-WebSocket (兼容性好，支持 CDN)
vmp="no"
# VLESS-XHTTP (新协议，防探测)
xhp="no"
# VLESS-XHTTP-ENC (带加密的 XHTTP)
vxp="no"
# VLESS-WebSocket-CDN (CDN 模式)
vwp="no"
# Shadowsocks-2022 (老牌协议)
ssp="no"
# Socks5 (基础代理)
sop="no"
# AnyTLS (Sing-box)
anp="no"
# Any-Reality (Sing-box Reality)
arp="no"

# ==========================================
# 端口配置
# 留空则随机生成端口
# ==========================================
port_vl_re=""    # VLESS-Reality 端口
port_hy2=""      # Hysteria2 端口 (UDP)
port_tu=""       # Tuic V5 端口 (UDP)

port_vm_ws=""    # VMess-WS 端口
port_xh=""       # VLESS-XHTTP 端口
port_vx=""       # VLESS-XHTTP-ENC 端口
port_vw=""       # VLESS-WS-CDN 端口
port_ss=""       # Shadowsocks 端口
port_so=""       # Socks5 端口
port_an=""       # AnyTLS 端口
port_ar=""       # Any-Reality 端口

# ==========================================
# 高级设置 (域名与路由)
# ==========================================

# Reality 偷取的目标域名 (默认: www.apple.com)
# 建议修改为适合您线路的知名国外域名
ym_vl_re="www.apple.com"

# 优选 CDN 域名 (可选)
# 用于 VMess-WS / VLESS-XHTTP 等支持 CDN 的协议
# 留空则自动生成或不使用
cdnym=""

# Xray 路由策略 (IPOnDemand / IPIfNonMatch)
# IPOnDemand: 域名解析出 IP 后匹配 (推荐)
xryx="IPOnDemand"

# WARP 路由策略 (通常同上)
wxryx="IPOnDemand"

# ==========================================
# Argo 隧道配置 (Cloudflare Tunnel)
# ==========================================

# 是否启用 Argo? (no / vmpt)
# vmpt: 启用 (需要开启 vmp 协议)
argo="no"

# Cloudflare Tunnel Token (固定隧道必填)
ARGO_AUTH=""

# Argo 域名 (固定隧道必填)
ARGO_DOMAIN=""

# ==========================================
# WARP 配置
# ==========================================

# WARP 模式 (no / sx / x6)
# sx: 全局接管 (所有流量走 WARP)
# x6: 仅 IPv6 优先 (让 IPv6 流量走 WARP)
# no: 不启用
warp="no"

# WARP 密钥 (手动模式必填)
WARP_IPV6=""
WARP_PRIVATE_KEY=""
WARP_RESERVED=""
